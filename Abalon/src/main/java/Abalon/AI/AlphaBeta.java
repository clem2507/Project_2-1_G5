package Abalon.AI;

public class AlphaBeta {

    /**
     * bot able to play using Alpha-Beta algorithm:
     * 1: make game tree (max nr of nodes, but impossible to have everything) >>GameTree class
     * 2: prune the tree using alpha-beta pruning (>>get less nodes)
     * 3: evaluate each node (evaluation function)
     * 4: bot applies first move
     * 5: opponent plays
     * 6: update game tree
     * 7: repeat
     */
}
